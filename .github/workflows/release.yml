name: Release

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Verify manifest version matches tag
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          MANIFEST_VERSION=$(node -p "require('./manifest.json').version")
          if [ "$TAG" != "$MANIFEST_VERSION" ]; then
            echo "❌ Version mismatch: tag=$TAG manifest=$MANIFEST_VERSION"
            exit 1
          fi
          echo "✅ Version match: $TAG"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: node esbuild.config.mjs
        env:
          HOME: /tmp  # redirect vault copy to a throwaway path

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/main.js
            dist/manifest.json
            dist/styles.css
            models/melspectrogram.onnx
            models/embedding_model.onnx
            models/hey_obsidian.onnx
          generate_release_notes: true
